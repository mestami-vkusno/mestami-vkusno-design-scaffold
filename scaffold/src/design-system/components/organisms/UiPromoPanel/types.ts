export interface UiPromoPanelProps {
  title: string
  /** Метка рядом с заголовком. */
  badge?: string
  description?: string
  /** Пояснение об ограничении под полем (с иконкой замка). */
  note?: string
}
