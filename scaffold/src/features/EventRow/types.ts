import type { EventCardProps } from '../EventCard/types'

/** Те же данные, что у карточки; строка компактнее: слева дата, справа название, заведение и цена. */
export type EventRowProps = EventCardProps
